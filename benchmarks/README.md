# Benchmarks (repo folder)

This folder is for benchmark datasets and (optionally) checked-in results.

- `benchmarks/datasets/`: JSON datasets used by `python -m contextbuddy bench`
- `benchmarks/results/`: local results (gitignored by default)

See `docs/benchmarks.md` for usage.

